<div class="row mb-3">
  <div class="col-md-12">
    <h1>
      All destinations
    </h1>

    <a href="/destinations/new" class="btn btn-block btn-outline-secondary">
      Add a new destination
    </a>
  </div>
</div>

<!-- Ransack Code start -->

<!-- Ransack Code end -->

<!-- Location Map Code start -->

<!-- Location Map Code end -->

<hr>

<div class="row mb-3">
  <div class="col-md-12">
    <table class="table">
      <tr>
        <th>
          ID
        </th>

        <th>
          City name
        </th>

        <th>
          Arrival date
        </th>

        <th>
          Departure date
        </th>

        <th>
          Trip
        </th>

        <th>
          Country name
        </th>

        <th>
          Created at
        </th>

        <th>
          Updated at
        </th>

        <th>
        </th>
      </tr>

      <% @destinations.each do |destination| %>
      <tr>
        <!-- Display destination.id start -->
        <td>
          <%= destination.id %>
        </td>
        <!-- Display destination.id end -->

        <!-- Display destination.city_name start -->
        <td>
          <%= destination.city_name %>
        </td>
        <!-- Display destination.city_name end -->

        <!-- Display destination.arrival_date start -->
        <td>
          <%= destination.arrival_date %>
        </td>
        <!-- Display destination.arrival_date end -->

        <!-- Display destination.departure_date start -->
        <td>
          <%= destination.departure_date %>
        </td>
        <!-- Display destination.departure_date end -->

        <!-- Display destination.trip_id start -->
        <td>
          <% if destination.trip.present? %>
            <a href="/trips/<%= destination.trip.id %>">
              <%= destination.trip.id %>
            </a>
          <% end %>
        </td>
        <!-- Display destination.trip_id end -->

        <!-- Display destination.country_name start -->
        <td>
          <%= destination.country_name %>
        </td>
        <!-- Display destination.country_name end -->

        <!-- Display destination.created_at start -->
        <td>
          <%= time_ago_in_words(destination.created_at) %> ago
        </td>
        <!-- Display destination.created_at end -->

        <!-- Display destination.updated_at start -->
        <td>
          <%= time_ago_in_words(destination.updated_at) %> ago
        </td>
        <!-- Display destination.updated_at end -->

        <td>
          <a href="/destinations/<%= destination.id %>">
            Show details
          </a>
        </td>
      </tr>
      <% end %>
    </table>

    <div class="row mb-3">
      <div class="col-md-12">
        <%= paginate @destinations, theme: "twitter-bootstrap-4" %>
      </div>
    </div>

  </div>
</div>
